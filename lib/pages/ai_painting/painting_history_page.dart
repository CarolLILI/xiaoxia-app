import 'package:flutter/material.dart';

/// 绘画历史页面（占位）
class PaintingHistoryPage extends StatelessWidget {
  const PaintingHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('作品库')),
      body: const Center(
        child: Text('历史作品功能开发中...'),
      ),
    );
  }
}
